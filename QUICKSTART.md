# 🚀 Быстрый старт SalaryMonitor

## Шаг 1: Подготовка базы данных (5 минут)

Откройте **SQL Server Management Studio (SSMS)** и выполните:

```sql
-- 1. Создайте структуру БД
-- Выполните файл: database/schema.sql

-- 2. Заполните тестовыми данными
-- Выполните файл: database/seed.sql

-- 3. Создайте SQL Views
-- Выполните файл: database/views.sql
```

✅ **Проверка**: Убедитесь, что БД `SalaryMonitor` создана и содержит таблицы.

---

## Шаг 2: Запуск Backend (3 минуты)

```bash
# 1. Перейдите в папку backend
cd backend

# 2. Установите зависимости
npm install

# 3. Создайте .env файл
cp .env.example .env

# 4. Отредактируйте .env (укажите свои данные БД и SAP)
# Минимально необходимо изменить:
#   DB_HOST, DB_USER, DB_PASSWORD
#   SAP_USERNAME, SAP_PASSWORD

# 5. Запустите сервер
npm run start:dev
```

✅ **Проверка**: Откройте http://localhost:3000 — должно появиться сообщение о запуске.

---

## Шаг 3: Запуск Frontend (2 минуты)

Откройте **новый терминал**:

```bash
# 1. Перейдите в папку frontend
cd frontend

# 2. Установите зависимости
npm install

# 3. Запустите приложение
npm run dev
```

✅ **Проверка**: Откройте http://localhost:5173 — должна появиться страница входа.

---

## Шаг 4: Тестовый вход (1 минута)

1. Откройте браузер: **http://localhost:5173/login**
2. Введите один из тестовых ШК:
   - `00000001` — обычный сотрудник (Иванов)
   - `00000002` — обычный сотрудник (Петров)
   - `00000099` — администратор склада

3. Нажмите **"Войти"**

✅ **Проверка**: Вы должны попасть на дашборд с данными о зарплате.

---

## Что дальше?

### Для разработки:

1. **Добавить реальных пользователей**:
```sql
INSERT INTO users (employee_id, fio, warehouse_id, role)
VALUES ('12345678', 'Новый Сотрудник', 1, 'employee');
```

2. **Настроить интеграцию с SAP**:
   - Проверьте учетные данные SAP в `.env`
   - Запустите ручную синхронизацию (TODO: добавить API endpoint)

3. **Изменить расписание синхронизации**:
```env
# В backend/.env
SYNC_CRON_SCHEDULE=0 3 * * *  # Каждый день в 03:00
```

### Для production:

См. документацию:
- `docs/INSTALLATION.md` — полная инструкция по установке
- `docs/API.md` — описание всех API endpoints
- `docs/ARCHITECTURE.md` — архитектура проекта

---

## Устранение проблем

### Backend не запускается

**Ошибка**: `Error: connect ECONNREFUSED 127.0.0.1:1433`

**Решение**:
1. Проверьте, что SQL Server запущен
2. Убедитесь, что TCP/IP включен в SQL Server Configuration Manager
3. Проверьте данные подключения в `.env`

### Frontend не видит Backend

**Ошибка**: `Network Error` или `CORS`

**Решение**:
1. Убедитесь, что backend запущен на порту 3000
2. Проверьте настройки CORS в `backend/src/main.ts`

### Нет данных на дашборде

**Решение**:
1. Убедитесь, что выполнен `database/seed.sql`
2. Проверьте, что пользователь с ШК `00000001` существует в БД
3. Проверьте логи backend в консоли

---

## Команды для разработки

### Backend

```bash
npm run start:dev     # Запуск с hot-reload
npm run build         # Сборка для production
npm run start:prod    # Запуск production версии
npm run lint          # Проверка кода
```

### Frontend

```bash
npm run dev           # Запуск dev-сервера
npm run build         # Сборка для production
npm run preview       # Предпросмотр production сборки
npm run lint          # Проверка кода
```

---

## Структура проекта

```
salary-monitor/
├── backend/              # NestJS API
│   ├── src/
│   │   ├── auth/        # Авторизация
│   │   ├── salary/      # Зарплата
│   │   ├── operations/  # Операции
│   │   ├── admin/       # Админ-панель
│   │   └── sap-integration/ # SAP OData
│   └── .env             # Конфигурация
│
├── frontend/            # React приложение
│   └── src/
│       ├── pages/       # Страницы
│       ├── components/  # Компоненты
│       └── services/    # API-клиент
│
├── database/            # SQL-скрипты
│   ├── schema.sql
│   ├── seed.sql
│   └── views.sql
│
└── docs/               # Документация
    ├── INSTALLATION.md
    ├── API.md
    └── ARCHITECTURE.md
```

---

## Полезные ссылки

- **Backend API**: http://localhost:3000/api
- **Frontend**: http://localhost:5173
- **SQL Views**: `v_salary_details`, `v_salary_by_day`, `v_salary_by_month`

---

## Контакты

По вопросам обращайтесь к системному администратору или разработчику.

**Версия**: 1.0.0  
**Дата**: 2026-01-11

