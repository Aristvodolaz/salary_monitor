# Архитектура SalaryMonitor

## 📐 Общая архитектура

```
┌─────────────────────────────────────────────────────────────┐
│                     Frontend (React)                        │
│  - Страницы: Login, Dashboard, Operations, Admin           │
│  - Material-UI компоненты                                   │
│  - Axios для HTTP запросов                                  │
└────────────────────────┬────────────────────────────────────┘
                         │ HTTP/REST API
                         ↓
┌─────────────────────────────────────────────────────────────┐
│                     Backend (NestJS)                        │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  Auth Module       │  Salary Module                  │   │
│  │  - JWT             │  - Расчет зарплаты              │   │
│  │  - Barcode login   │  - Статистика                   │   │
│  ├──────────────────────────────────────────────────────┤   │
│  │  Operations Module │  Admin Module                   │   │
│  │  - Список операций │  - Управление складом           │   │
│  │  - Группировка     │  - Экспорт CSV                  │   │
│  ├──────────────────────────────────────────────────────┤   │
│  │  SAP Integration Module                              │   │
│  │  - OData клиент     - Планировщик (cron)            │   │
│  │  - Синхронизация    - Логирование                   │   │
│  └──────────────────────────────────────────────────────┘   │
└────────────────────────┬────────────────────────────────────┘
                         │ SQL Queries
                         ↓
┌─────────────────────────────────────────────────────────────┐
│                  MS SQL Server Database                     │
│  - Таблицы: users, operations, tariffs, warehouses         │
│  - Views: v_salary_details, v_salary_by_day                │
│  - Индексы для производительности                           │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ↑ OData API (ежедневная синхронизация)
                         │
┌─────────────────────────────────────────────────────────────┐
│                      SAP WMS System                         │
│  - OData API: Z_REP_MON_ORDERS_SRV                         │
│  - Данные об операциях складов                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 🏗️ Backend Архитектура (NestJS)

### Модульная структура

```
backend/src/
├── auth/                # Авторизация
│   ├── auth.module.ts
│   ├── auth.service.ts   # Логика входа по ШК
│   ├── auth.controller.ts
│   ├── strategies/
│   │   └── jwt.strategy.ts
│   └── dto/
│       └── login-barcode.dto.ts
│
├── users/               # Пользователи
│   ├── users.module.ts
│   ├── users.service.ts  # CRUD пользователей
│   └── users.controller.ts
│
├── salary/              # Зарплата
│   ├── salary.module.ts
│   ├── salary.service.ts # Расчет зарплаты
│   ├── salary.controller.ts
│   └── dto/
│
├── operations/          # Операции
│   ├── operations.module.ts
│   ├── operations.service.ts
│   ├── operations.controller.ts
│   └── dto/
│
├── admin/               # Админ-панель
│   ├── admin.module.ts
│   ├── admin.service.ts  # Управление складом
│   ├── admin.controller.ts
│   └── dto/
│
├── sap-integration/     # Интеграция с SAP
│   ├── sap-integration.module.ts
│   ├── sap-integration.service.ts  # OData клиент
│   └── sap-scheduler.service.ts    # Cron-задачи
│
├── database/            # База данных
│   ├── database.module.ts
│   └── database.service.ts  # MS SQL connection pool
│
├── common/              # Общие компоненты
│   ├── decorators/      # @CurrentUser, @Roles
│   ├── guards/          # JwtAuthGuard, RolesGuard
│   └── logger/          # Winston logger
│
├── app.module.ts        # Главный модуль
└── main.ts              # Entry point
```

### Принципы проектирования

1. **Модульность**: Каждая фича — отдельный модуль
2. **Dependency Injection**: NestJS DI контейнер
3. **Separation of Concerns**: Controller → Service → Database
4. **Guards & Interceptors**: Авторизация и логирование

---

## 🎨 Frontend Архитектура (React)

### Структура компонентов

```
frontend/src/
├── components/          # Переиспользуемые компоненты
│   └── Layout.tsx       # Навигация, AppBar
│
├── pages/               # Страницы
│   ├── LoginPage.tsx    # Авторизация
│   ├── DashboardPage.tsx # Дашборд зарплаты
│   ├── OperationsPage.tsx # Таблица операций
│   └── AdminPage.tsx    # Админ-панель
│
├── services/            # API-клиент
│   └── api.ts           # Axios instance + endpoints
│
├── context/             # Context API
│   └── AuthContext.tsx  # Глобальное состояние юзера
│
├── theme.ts             # Material-UI тема
├── App.tsx              # Роутинг
└── main.tsx             # Entry point
```

### State Management

- **Auth**: React Context API
- **Server State**: Прямые вызовы API (можно заменить на React Query)
- **Local State**: useState + useEffect

---

## 🗄️ База данных (MS SQL)

### ER-диаграмма

```
┌─────────────────┐
│   warehouses    │
│─────────────────│       ┌─────────────────┐
│ id (PK)         │◄──────│     users       │
│ code (UNIQUE)   │       │─────────────────│
│ name            │       │ id (PK)         │
└─────────────────┘       │ employee_id (UK)│
                          │ fio             │
┌─────────────────┐       │ warehouse_id(FK)│
│    tariffs      │       │ role            │
│─────────────────│       └────────┬────────┘
│ id (PK)         │                │
│ warehouse_code  │                │
│ operation_type  │                │
│ rate            │       ┌────────▼────────┐
│ valid_from      │       │   operations    │
│ valid_to        │       │─────────────────│
└─────────────────┘       │ id (PK)         │
                          │ user_id (FK)    │
┌─────────────────┐       │ warehouse_code  │
│ quality_matrix  │       │ operation_type  │
│─────────────────│       │ count           │
│ errors_count(PK)│       │ operation_date  │
│ k_value         │       │ amount          │
└─────────────────┘       │ sap_order_id    │
                          └─────────────────┘
┌─────────────────┐
│ salary_summary  │       ┌─────────────────┐
│─────────────────│       │   sync_logs     │
│ id (PK)         │       │─────────────────│
│ user_id (FK)    │       │ id (PK)         │
│ period_start    │       │ warehouse_code  │
│ period_end      │       │ sync_start      │
│ total_amount    │       │ sync_end        │
└─────────────────┘       │ status          │
                          │ records_processed│
                          └─────────────────┘
```

### Ключевые индексы

```sql
-- Поиск пользователя по ШК (частый запрос)
CREATE INDEX idx_users_employee_id ON users(employee_id);

-- Фильтрация операций по дате (для расчета зарплаты)
CREATE INDEX idx_operations_user_date ON operations(user_id, operation_date);

-- Группировка по типу операций
CREATE INDEX idx_operations_warehouse ON operations(warehouse_code);

-- Поиск тарифа по складу и типу
CREATE INDEX idx_tariffs_warehouse_operation ON tariffs(warehouse_code, operation_type);
```

### SQL Views (для производительности)

```sql
-- v_salary_details: детальный расчет зарплаты
-- Объединяет operations + users + warehouses + tariffs

-- v_salary_by_day: агрегация по дням
-- Группирует операции по датам

-- v_salary_by_month: агрегация по месяцам
-- Группирует операции по месяцам
```

---

## 🔄 Поток данных (Data Flow)

### 1. Синхронизация с SAP (ежедневно, 02:00)

```
┌─────────────┐     GET OData     ┌──────────────┐
│ SAP WMS API │◄─────────────────►│ NestJS Cron  │
└─────────────┘                   └──────┬───────┘
                                         │ Parse XML
                                         ↓
                                  ┌──────────────┐
                                  │ Operations   │
                                  │ (INSERT/     │
                                  │  UPDATE)     │
                                  └──────┬───────┘
                                         ↓
                                  ┌──────────────┐
                                  │ MS SQL DB    │
                                  └──────────────┘
```

**Шаги:**
1. Планировщик запускается в 02:00 (node-cron)
2. Для каждого склада (01SS, 02DQ и т.д.):
   - Формируется OData запрос за последние 6 месяцев
   - Axios отправляет GET запрос с Basic Auth
   - Парсится XML/JSON ответ
   - Данные сохраняются в `operations`
3. Логируется статус в `sync_logs`

### 2. Авторизация пользователя

```
┌──────────────┐   POST /auth/barcode   ┌───────────────┐
│ Frontend     │───────────────────────►│ Auth Service  │
│ (LoginPage)  │                        └───────┬───────┘
└──────────────┘                                │ Query DB
                                                ↓
                                        ┌───────────────┐
                                        │ users table   │
                                        └───────┬───────┘
                                                │ Found
                                                ↓
┌──────────────┐   JWT Token + User     ┌───────────────┐
│ Frontend     │◄───────────────────────│ Auth Service  │
│ (Save token) │                        │ (Sign JWT)    │
└──────────────┘                        └───────────────┘
```

### 3. Получение зарплаты

```
┌──────────────┐   GET /salary?period=month   ┌─────────────┐
│ Dashboard    │─────────────────────────────►│ Salary      │
│              │   (JWT in header)            │ Service     │
└──────────────┘                              └──────┬──────┘
                                                     │ Query
                                                     ↓
                                              ┌──────────────┐
                                              │v_salary_by   │
                                              │_month VIEW   │
                                              └──────┬───────┘
                                                     │ Aggregate
                                                     ↓
┌──────────────┐   JSON { total_amount, ... } ┌──────────────┐
│ Dashboard    │◄─────────────────────────────│ Salary       │
│ (Render)     │                              │ Service      │
└──────────────┘                              └──────────────┘
```

---

## 🔐 Безопасность

### Аутентификация

- **JWT**: 24-часовой access token (настраивается в .env)
- **No refresh token**: Для простоты (можно добавить)
- **Barcode login**: Только по employee_id (без пароля)

### Авторизация

```typescript
// Пример защищенного роута
@UseGuards(JwtAuthGuard, RolesGuard)
@Roles('admin')
@Get('admin/employees')
async getEmployees() { ... }
```

- **JwtAuthGuard**: Проверяет валидность токена
- **RolesGuard**: Проверяет роль пользователя
- **@Roles('admin')**: Декоратор для указания требуемой роли

### Потенциальные улучшения

1. **Rate Limiting**: Ограничение запросов на авторизацию
2. **HTTPS**: В production обязательно
3. **Helmet**: Защита от известных веб-уязвимостей
4. **CSRF**: Если используются cookies

---

## 🚀 Масштабируемость

### Текущие ограничения

- **Однопоточность**: Node.js single-threaded
- **Без кэширования**: Каждый запрос идет в БД

### Решения для масштабирования

1. **Redis кэш**:
```typescript
// Кэширование расчета зарплаты на 5 минут
const cached = await redis.get(`salary:${userId}:${period}`);
if (cached) return JSON.parse(cached);
```

2. **Connection Pooling**:
```typescript
pool: {
  max: 50,  // Увеличить для production
  min: 5,
  idleTimeoutMillis: 30000,
}
```

3. **Load Balancing**:
```
       ┌──────────────┐
       │   Nginx LB   │
       └──────┬───────┘
              │
    ┌─────────┼─────────┐
    ↓         ↓         ↓
┌────────┐┌────────┐┌────────┐
│NestJS 1││NestJS 2││NestJS 3│
└───┬────┘└───┬────┘└───┬────┘
    └─────────┼─────────┘
              ↓
        ┌──────────┐
        │ MS SQL   │
        └──────────┘
```

4. **Database Optimization**:
   - Партиционирование `operations` по датам
   - Архивация старых данных (> 1 года)

---

## 📊 Мониторинг и Логирование

### Winston Logger

```typescript
logger.log('✅ Подключение к MS SQL установлено');
logger.error('❌ Ошибка синхронизации с SAP', error.stack);
```

**Логи сохраняются**:
- `logs/application-YYYY-MM-DD.log` (все уровни)
- `logs/error-YYYY-MM-DD.log` (только ошибки)

### Метрики для мониторинга

1. **API Response Time**: Среднее время ответа endpoints
2. **SAP Sync Status**: Успех/провал ежедневной синхронизации
3. **Database Connection Pool**: Активные соединения
4. **Error Rate**: Количество ошибок 500/4xx

**Рекомендации**:
- Prometheus + Grafana для метрик
- Sentry для отслеживания ошибок

---

## 🧪 Тестирование

### Структура тестов (для будущей реализации)

```
backend/test/
├── unit/
│   ├── auth.service.spec.ts
│   ├── salary.service.spec.ts
│   └── sap-integration.service.spec.ts
├── integration/
│   ├── auth.e2e.spec.ts
│   └── salary.e2e.spec.ts
└── fixtures/
    └── sample-odata-response.xml
```

### Команды

```bash
npm run test          # Unit tests
npm run test:e2e      # E2E tests
npm run test:cov      # Coverage report
```

---

## 📦 Deployment

### Рекомендуемая архитектура для production

```
Internet
   │
   ↓
┌────────────────────┐
│  Nginx (SSL/TLS)   │
│  - Reverse Proxy   │
│  - Static files    │
└─────────┬──────────┘
          │
          ├─────────► Frontend (Static)
          │
          └─────────► Backend API (PM2)
                      └─────► MS SQL Server
```

---

Эта архитектура обеспечивает:
- ✅ Модульность и поддерживаемость
- ✅ Разделение ответственности
- ✅ Масштабируемость
- ✅ Безопасность
- ✅ Производительность

